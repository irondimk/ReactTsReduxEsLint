import * as React from 'react';
import './App.css';

export default function App() {
    return (
        <div>
            <div>
                Configure React, Redux, React-Redux, TypeScript, ESLint, AXIOS,
            </div>
        </div>
    );
}
